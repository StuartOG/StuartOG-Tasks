#program to find times tables

integer = int(input("Input an integer: ")) #inputs an integer
counter=1 #starts counter at 1
while counter <= 10: #while loop to keep outputting until counter is bigger than ten
    print(counter*integer)
    counter += 1 # used to stop the while loop, every time the counter gains one